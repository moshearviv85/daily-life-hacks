# Project Status — 2026-04-27 (handoff for new session)

Read this file first when opening a fresh session. It captures the state at the end of the cleanup work, what's still open, and the corrections Claude made to itself along the way.

---

## TL;DR

The cleanup phase finished. `scripts/`, `pipeline-data/`, the project root, and the article writer DB are all organized. The enforcement infrastructure (rules + hooks + skills) was hardened with two new layers (`execution-guard`, `memory-guard`). The 50 production articles are physically separated from the 92 legacy ones. **The user's plan from here: verify pipeline end-to-end → swap unpublished pipeline articles with the 50 → next batch of 50.**

The user is non-technical, communicates in Hebrew, runs the site as a content/affiliate business solo. He's the renovation contractor on the project profile. Always defer to him on intent.

---

## What's done (verified by tool calls)

### Codebase organization

- `scripts/` cleaned: 13 active `.py` files at root, all matching the stage map in `pipeline-data/PIPELINE.md`. Archive holds 22 orphans + 1 deprecated stub.
- `scripts/stage_1_5/` is **active** despite earlier mistake — it's imported by `write.py`, `generate_hero_brief.py`, `generate_pin_briefs.py`, and `stage_1_75/judge.py`. Do NOT archive.
- `pipeline-data/`: `add_briefs.py` and `pipeline.db` (dead older DB) moved to `pipeline-data/archive/`.
- Project root: only `CLAUDE.md` and `README.md` remain. Everything else (n8n/, publer published/, template/, CHANGELOG.md, CLAUDE.md.backup-2026-04-22, SPEC-ENFORCEMENT-ARCHITECTURE.md, cols.json, .env.bak.before-telegram-removal) → `archive/2026-04-25-cleanup/`.
- `kinetic-video-bundle/` (1.34 GB) and `scripts/adspower/` stay in place per user instruction.

### DB — `pipeline-data/topic-research.sqlite`

- `write_outputs` cleaned from 66 → 50 rows. 16 removed: 13 early-run retries (runs 1-4 before SPEC was final) + 3 disqualified rows.
- Backup of pre-cleanup DB at `pipeline-data/topic-research.sqlite.backup-2026-04-26` (untracked, gitignored).
- `stage2_output` still has 100 candidate topics (the 20:15:15 quota balance run = run_id 12).

### Articles directory split (2026-04-27)

- `src/data/articles/` — **92 legacy articles, in git, live on the site.** Untouched.
- `src/data/articles/production/` — **50 new SPEC-written articles, NOT in git, not on the live site.** The DB `write_outputs` table is the authoritative list of which 50.
- Astro build verified: `npm run build` → 303 pages, no errors. Astro's glob pattern `*.md` happens to match recursively, so production/ articles also build locally. They appear under URLs `/{slug}` (slug-based, no folder prefix). On Cloudflare they will not appear until committed.

### Enforcement (.claude/)

Active hooks (registered in `.claude/settings.json`):

1. **`content-checker.py`** (PreToolUse Edit|Write) — blocks em-dash / medical claims / supplements / banned AI words on `src/data/articles/` paths.
2. **`memory-guard.py`** (PreToolUse Write|Edit|MultiEdit) — caps `MEMORY.md` at 60 lines / 200 chars per line, blocks numeric snapshots, blocks credential-shaped strings.
3. **`execution-guard.py`** (PreToolUse Write|Edit|MultiEdit) — blocks edits to `*.py / *.ts / *.js / *.tsx / *.astro / *.sql / *.yml / *.yaml` and to `scripts/`, `src/`, `functions/` paths unless the last user message contains an approval marker (`yes / go / אישור / תבצע / כן`) or a task-assignment verb (`write/fix/build/תכתוב/תקן`).
4. **`post-tool-state.py`** (PostToolUse) — writes the last 30 tool calls to `.claude/state.json`. Verified working: state file updates correctly under key `tool_calls`.
5. **`completion-evidence-gate.py`** (Stop) — checks Hebrew + English completion claims against `state.json` evidence and git status. Blocks if claim has no backing.
6. **`instructions-logger.py`** (InstructionsLoaded) — logs rule loads to `.claude/logs/instructions.log`.

Always-on rules (.claude/rules/, all auto-loaded):

- `truth.md` — 7 rules. Notably #6 (factual claims need verification in THIS conversation) and #7 (every task has a verification command).
- `content.md` — em-dash, medical, supplements, AI-words bans for content.
- `memory.md` — MEMORY.md is index only, default = update existing sub-file.

Skills:

- `sdd` — Spec-Driven Development (open with the SPEC, plan with file paths + verification commands, no code without approval).
- `tdd` — Test-Driven Development (RED → GREEN → REFACTOR with tool evidence per step).
- `write-article`, `david-miller-voice`, `kinetic-video`, `post-pin` — content-side workflows.

CLAUDE.md Autonomy section: rewritten 2026-04-26. Default = ASK before any edit on code paths or external action. Listed exceptions: explicit approval marker, named task verb, active SDD/TDD plan, read-only operations. The single biggest behavioral fix.

### Documentation (read order)

1. **`pipeline-data/PIPELINE.md`** — canonical map of 11 pipeline stages with active script paths, CLI examples, DB tables, status. Always check this before adding a new script.
2. **`docs/inventory-2026-04-26.md`** — full project inventory snapshot (scripts categorized, articles dir, top-level dirs).
3. **`docs/specs/2026-04-26-pipeline-v2.md`** — drafted by parallel session, not yet executed. Plans a parallel `pipeline_v2/` dir for safer iteration.
4. **`docs/specs/2026-04-25-visual-brief-split.md`** — SPEC for splitting visual_brief into hero + pin scripts. Done.

---

## Open items

### Active user plan (no work needed by Claude unless asked)

1. **Verify pipeline end-to-end** — user finished the image script in a parallel session and wants to confirm the whole chain works (topic research → write → validate → judge → visual brief → image gen → pin gen).
2. **Swap unpublished pipeline articles with the 50 in `src/data/articles/production/`** — when smoke test confirms.
3. **Next batch of 50 articles** with the same SPEC depth.

### Pipeline_v2 SPEC

Status: drafted, NOT executed. Will create a parallel `pipeline_v2/` directory copying active scripts so legacy `scripts/` can be cleaned more aggressively without touching live code. Wait for explicit user approval before executing.

### Articles directory restructure

Already partially done: 50 in `src/data/articles/production/`, 92 in `src/data/articles/`. Astro picks up both via the glob pattern but only the 92 are in git. **No further restructure needed unless the user asks.**

### Open uncertainties (user said "leave if unsure")

- `kinetic-video-bundle/` (1.34 GB) — user undecided on its future. Stays at root.
- `scripts/adspower/` — active, user is working on it. Stays.

### Parallel session has uncommitted work

These show as `M` or `??` in git status — user asked not to commit on his behalf:

- `M scripts/generate-pinterest-pins.py`
- `M scripts/topic_research/db.py`
- `M scripts/topic_research/stage1.py`
- `M tests/topic_research/test_stage1.py`
- `?? docs/specs/` (the v2 SPEC and visual-brief-split SPEC)

Let the parallel session commit these when it's done. If a new session is opened mid-stream, do not auto-stage them.

---

## Corrections made in this session (do not repeat)

Per `truth.md` rule #6, recording the false claims I caught and fixed so future sessions don't make the same mistakes.

1. **State.json claim was wrong.** Earlier diagnostic accused `state.json` of having empty history. The hook actually writes to key `tool_calls`, not `history`. The `completion-evidence-gate` works as designed. Don't accuse the hook again without checking the key it writes.
2. **`scripts/stage_1_5/` was wrongly archived.** First orphan-sweep grep was scoped to subdirs only and missed the four root-level importers (`write.py`, `generate_hero_brief.py`, `generate_pin_briefs.py`, `stage_1_75/judge.py`). Restored. PIPELINE.md updated to mark it as ACTIVE with all four importers named.
3. **Astro does not hide subfolder articles.** I claimed `pattern: '*.md'` excludes subfolders. Build proved otherwise — production/ articles built locally too. The "draft" separation works only via git, not via folder structure.
4. **`write_outputs` row count was reported wrong.** Said "66 rows of generated articles" when the unique-slug count was 50 (16 rows were retries, 3 were DQ). Always `COUNT(DISTINCT slug)` when slug is the entity, not raw `COUNT(*)`.

---

## SPEC vocabulary (user correction, 2026-04-27)

When the user says "let's write a SPEC", he means: **specify the product** (features, inputs/outputs, characteristics, acceptance criteria). NOT: analyze the pain or interrogate why we want it.

The pain-framing pattern was an accidental rule in my memory. **Do not open a SPEC with a "## Problem" section. Do not ask "what's the pain". The user provides the requirement; the SPEC describes what to build.**

`feedback_research_and_spec.md` rule #2 and `.claude/skills/sdd/SKILL.md` need updating to remove the "Problem first" framing. (Proposed in the prior conversation; not yet edited because user did not give explicit go-ahead.)

---

## Recent commits (in this session)

```
0a03e34  fix(claude): close execution-rule gap + cleanup root .md noise
5597bbc  chore(cleanup): archive 22 orphan scripts + pipeline.db + memory backup, add PIPELINE.md and inventory
bcc3a4e  fix(cleanup): revert mistaken archive of scripts/stage_1_5/
5a38341  chore: gitignore __pycache__ and untrack accidentally committed pyc files
72f888d  chore: stage the now-deleted scripts/archive/stage_1_5/ paths
5885f82  chore: archive n8n/ workflows directory
39f0d71  chore: archive publer + 6 root-level items, gitignore .env.bak*
```

The article move (`production/` subfolder) is **not yet committed** because the 50 articles were never tracked. Move is reflected on disk only; git ls-files still shows them as untracked.

---

## Quick health check for next session

Before doing anything, run these to confirm the world is sane:

```bash
# DB row counts (should be 50)
python -c "import sqlite3; print(sqlite3.connect('pipeline-data/topic-research.sqlite').execute('SELECT COUNT(*) FROM write_outputs WHERE status=\"written\" AND disqualified=0').fetchone()[0])"

# Article counts
ls src/data/articles/*.md | wc -l         # should be 92
ls src/data/articles/production/*.md | wc -l   # should be 50

# Build still works
npm run build   # should produce ~303 pages

# Active scripts in scripts/ root
ls scripts/*.py   # should be 13 files, all from PIPELINE.md
```

If any of these come back surprising, something drifted — investigate before assuming.
